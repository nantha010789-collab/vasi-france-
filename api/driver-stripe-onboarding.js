const u=process.env.VASI_SUPABASE_URL||process.env.SUPABASE_URL;const k=process.env.VASI_SUPABASE_ANON_KEY||process.env.SUPABASE_ANON_KEY;const sk=process.env.STRIPE_SECRET_KEY;const ALLOWED_COUNTRIES=new Set(['FR','GB','BE','DE','NL','LU','CH','ES','IT','PT','IE','AT']);
async function sb(path,auth,opt={}){return fetch(`${u}${path}`,{...opt,headers:{apikey:k,Authorization:auth,'Content-Type':'application/json',...(opt.headers||{})}})}
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'POST required'});res.setHeader('Cache-Control','no-store');const auth=req.headers.authorization||'';if(!auth.startsWith('Bearer '))return res.status(401).json({error:'Unauthorized'});if(!u||!k||!sk)return res.status(503).json({error:'Stripe/Supabase environment is not configured'});try{const me=await sb('/auth/v1/user',auth,{headers:{apikey:k}});const user=await me.json().catch(()=>null);if(!me.ok||!user?.id)return res.status(401).json({error:'Unauthorized'});const dr=await sb(`/rest/v1/drivers?select=id,user_id,full_name,phone,verified,status,stripe_account_id&user_id=eq.${encodeURIComponent(user.id)}&limit=1`,auth);const drivers=await dr.json().catch(()=>[]);if(!dr.ok||!drivers?.length)return res.status(404).json({error:'Driver profile required'});const d=drivers[0];if(!d.verified||d.status!=='approved')return res.status(403).json({error:'Driver must be approved and verified before Stripe onboarding'});const country=String(req.body?.country||'FR').trim().toUpperCase();if(!ALLOWED_COUNTRIES.has(country))return res.status(400).json({error:'Unsupported driver payout country'});let accountId=d.stripe_account_id;if(!accountId){const p=new URLSearchParams();p.set('controller[fees][payer]','application');p.set('controller[losses][payments]','application');p.set('controller[stripe_dashboard][type]','express');p.set('country',country);if(d.full_name)p.set('business_profile[name]',String(d.full_name).slice(0,120));if(d.phone)p.set('business_profile[support_phone]',String(d.phone).slice(0,40));const ar=await fetch('https://api.stripe.com/v1/accounts',{method:'POST',headers:{Authorization:`Bearer ${sk}`,'Content-Type':'application/x-www-form-urlencoded','Idempotency-Key':`vasi-driver-account-${d.id}`},body:p.toString()});const a=await ar.json();if(!ar.ok)return res.status(ar.status).json({error:a?.error?.message||'Stripe account creation failed'});accountId=a.id;const up=await sb(`/rest/v1/drivers?id=eq.${encodeURIComponent(d.id)}`,auth,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify({stripe_account_id:accountId})});if(!up.ok)return res.status(500).json({error:'Stripe account created but driver record could not be updated'});}const rawBase=process.env.VASI_PUBLIC_URL||'https://vasi-new.vercel.app';let base;try{const parsed=new URL(rawBase);if(!['https:','http:'].includes(parsed.protocol))throw new Error('bad');base=parsed.origin}catch{return res.status(503).json({error:'VASI public URL is not configured correctly'})}const p2=new URLSearchParams();p2.set('account',accountId);p2.set('refresh_url',`${base}/driver.html?stripe=refresh`);p2.set('return_url',`${base}/driver.html?stripe=complete`);p2.set('type','account_onboarding');const lr=await fetch('https://api.stripe.com/v1/account_links',{method:'POST',headers:{Authorization:`Bearer ${sk}`,'Content-Type':'application/x-www-form-urlencoded'},body:p2.toString()});const link=await lr.json();if(!lr.ok)return res.status(lr.status).json({error:link?.error?.message||'Stripe onboarding link failed'});return res.status(200).json({account_id:accountId,url:link.url});}catch(e){return res.status(500).json({error:e?.message||'Server error'})}}
